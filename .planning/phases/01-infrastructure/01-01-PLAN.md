---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .env.example
  - .env
  - .gitignore
  - src/index.ts
  - src/config.ts
  - prisma/schema.prisma
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "Project compiles with TypeScript without errors"
    - "All environment variables are documented in .env.example"
    - "Database schema exists and migrations run successfully"
    - "User data persists in the database across process restarts"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "grammY"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
    - path: "src/config.ts"
      provides: "Typed environment configuration"
      exports: ["config"]
    - path: "src/index.ts"
      provides: "Application entry point"
    - path: "prisma/schema.prisma"
      provides: "Database schema with User and EnergyLog models"
      contains: "model User"
    - path: ".env.example"
      provides: "Environment variable documentation"
  key_links:
    - from: "src/config.ts"
      to: ".env"
      via: "dotenv loading"
      pattern: "process\\.env"
    - from: "src/index.ts"
      to: "src/config.ts"
      via: "import"
      pattern: "import.*config"
---

<objective>
Set up the project foundation: TypeScript project with all dependencies, environment config, database schema, and working persistence layer.

Purpose: Every subsequent plan depends on a working project structure with typed config and database.
Output: Compilable TypeScript project with Prisma database, ready for bot and API integration.
</objective>

<execution_context>
@/Users/timur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize TypeScript project with dependencies</name>
  <files>package.json, tsconfig.json, .gitignore, .env.example, .env, src/config.ts</files>
  <action>
1. Run `git init` in project root (if not already a repo).
2. Create package.json with:
   - name: "energy-bot"
   - scripts: "build": "tsc", "start": "node dist/index.js", "dev": "tsx watch src/index.ts", "db:push": "prisma db push", "db:generate": "prisma generate"
   - Dependencies: grammy, @prisma/client, @anthropic-ai/sdk, node-cron, dotenv
   - DevDependencies: typescript, tsx, prisma, @types/node, @types/node-cron

3. Run `npm install`.

4. Create tsconfig.json:
   - target: ES2022, module: NodeNext, moduleResolution: NodeNext
   - outDir: ./dist, rootDir: ./src
   - strict: true, esModuleInterop: true, skipLibCheck: true

5. Create .gitignore with: node_modules/, dist/, .env, *.db, *.db-journal

6. Create .env.example documenting ALL required env vars:
   ```
   TELEGRAM_BOT_TOKEN=         # From @BotFather
   ANTHROPIC_API_KEY=          # From console.anthropic.com
   DATABASE_URL=file:./data/energy.db
   WEBAPP_URL=                 # Mini App URL (HTTPS required)
   ```

7. Create .env with same vars (empty values except DATABASE_URL=file:./data/energy.db).

8. Create src/config.ts:
   - Load dotenv at top
   - Export typed config object reading from process.env
   - Validate that required vars exist at startup (throw if TELEGRAM_BOT_TOKEN missing)
   - Fields: telegramBotToken, anthropicApiKey, databaseUrl, webappUrl
  </action>
  <verify>
    <automated>cd /Users/timur/energy-management && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Project compiles, all dependencies installed, config loads env vars with validation</done>
</task>

<task type="auto">
  <name>Task 2: Database schema and persistence layer</name>
  <files>prisma/schema.prisma, src/db.ts, src/index.ts</files>
  <action>
1. Create prisma/schema.prisma with:
   - datasource: sqlite, url from env DATABASE_URL
   - generator: prisma-client-js

   Models:
   - User: id (Int autoincrement), telegramId (BigInt unique), firstName (String), lastName (String?), username (String?), createdAt (DateTime default now), updatedAt (DateTime updatedAt)
   - EnergyLog: id (Int autoincrement), userId (Int, relation to User), physical (Int), mental (Int), emotional (Int), spiritual (Int), note (String?), logType (String — "morning" | "evening" | "manual"), createdAt (DateTime default now)
   - Relation: User has many EnergyLogs, EnergyLog belongs to User

2. Run `npx prisma generate` and `npx prisma db push`.

3. Create src/db.ts:
   - Export a singleton PrismaClient instance
   - Export helper: findOrCreateUser(telegramId: bigint, firstName: string, lastName?: string, username?: string) => Promise<User>

4. Create src/index.ts as minimal entry point:
   - Import config, db
   - Log "EnergyBot starting..."
   - Call prisma.$connect() to verify DB connection
   - Log "Database connected"
   - For now, just exit cleanly (bot setup comes in plan 01-02)

5. Verify: run `npx tsx src/index.ts` — should print starting + connected messages without errors.
  </action>
  <verify>
    <automated>cd /Users/timur/energy-management && npx prisma db push --accept-data-loss 2>&1 && npx tsx -e "import { PrismaClient } from '@prisma/client'; const p = new PrismaClient(); async function main() { await p.\$connect(); const u = await p.user.create({ data: { telegramId: 12345n, firstName: 'Test' } }); console.log('Created:', u.id); const found = await p.user.findUnique({ where: { telegramId: 12345n } }); console.log('Found:', found?.firstName); await p.user.delete({ where: { id: u.id } }); console.log('PASS'); await p.\$disconnect(); } main();"</automated>
  </verify>
  <done>Database schema applied, User and EnergyLog models exist, data persists (create+read verified), project compiles cleanly</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `npx prisma db push` — schema applied without errors
3. Create and read a User record — data persists
4. `npx tsx src/index.ts` — prints startup messages, exits cleanly
</verification>

<success_criteria>
- TypeScript project compiles without errors
- All dependencies installed (grammy, prisma, anthropic SDK, node-cron)
- Database schema has User and EnergyLog models
- Data written to DB persists (verified by create + read test)
- Environment configuration is typed and validated
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>

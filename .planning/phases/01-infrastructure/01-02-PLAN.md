---
phase: 01-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/bot.ts
  - src/index.ts
  - src/handlers/start.ts
  - src/handlers/help.ts
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02

user_setup:
  - service: telegram
    why: "Bot token and Mini App WebApp URL required"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram @BotFather -> /newbot -> copy token"
      - name: WEBAPP_URL
        source: "Your HTTPS URL where Mini App will be hosted (can be placeholder for now)"
    dashboard_config:
      - task: "Create bot via @BotFather with /newbot"
        location: "Telegram -> @BotFather"
      - task: "Set Mini App URL via /newapp or /setmenubutton"
        location: "Telegram -> @BotFather"

must_haves:
  truths:
    - "Sending /start to the bot returns a welcome message"
    - "Sending any text to the bot returns an echo/acknowledgment"
    - "The bot has a menu button that opens the Mini App WebApp URL"
  artifacts:
    - path: "src/bot.ts"
      provides: "grammY bot instance and middleware setup"
      exports: ["bot"]
    - path: "src/handlers/start.ts"
      provides: "Handler for /start command"
      exports: ["startHandler"]
    - path: "src/index.ts"
      provides: "Bot startup with polling"
  key_links:
    - from: "src/index.ts"
      to: "src/bot.ts"
      via: "import and bot.start()"
      pattern: "bot\\.start"
    - from: "src/bot.ts"
      to: "src/handlers/start.ts"
      via: "bot.command registration"
      pattern: "bot\\.command.*start"
    - from: "src/bot.ts"
      to: "src/db.ts"
      via: "user registration in /start handler"
      pattern: "findOrCreateUser"
---

<objective>
Connect the Telegram Bot API and register the Mini App WebApp. The bot should respond to messages and have a menu button for the Mini App.

Purpose: INFRA-01 (bot reachable and responding) and INFRA-02 (Mini App URL opens in Telegram).
Output: Running bot that responds to /start, echoes messages, and has Mini App menu button configured.
</objective>

<execution_context>
@/Users/timur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bot instance and command handlers</name>
  <files>src/bot.ts, src/handlers/start.ts, src/handlers/help.ts, src/index.ts</files>
  <action>
1. Create src/handlers/start.ts:
   - Export async function startHandler(ctx: CommandContext) that:
     - Extracts telegram user info from ctx.from
     - Calls findOrCreateUser(telegramId, firstName, lastName, username) from db.ts
     - Replies with welcome message in Russian: "Привет, {firstName}! Я EnergyBot — помогу отслеживать твои 4 типа энергии. Используй кнопку меню, чтобы открыть приложение."
     - If WEBAPP_URL is set, include an InlineKeyboard button with web_app URL

2. Create src/handlers/help.ts:
   - Export async function helpHandler that replies with available commands list

3. Create src/bot.ts:
   - Import Bot from grammy
   - Import config
   - Create and export bot instance: new Bot(config.telegramBotToken)
   - Register /start command -> startHandler
   - Register /help command -> helpHandler
   - Register fallback handler (bot.on("message:text")) that echoes: "Получил: {text}. Скоро научусь отвечать умнее!"
   - Set bot commands via bot.api.setMyCommands([{command: "start", description: "Начать"}, {command: "help", description: "Помощь"}])
   - If config.webappUrl is set, call bot.api.setChatMenuButton({ menu_button: { type: "web_app", text: "Energy App", web_app: { url: config.webappUrl } } }) during bot setup

4. Update src/index.ts:
   - Import bot from bot.ts
   - After DB connect, start bot with bot.start({ onStart: () => console.log("Bot is running") })
   - Add graceful shutdown: process.on("SIGINT", () => { bot.stop(); prisma.$disconnect() })
  </action>
  <verify>
    <automated>cd /Users/timur/energy-management && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Bot module compiles, handlers registered, /start saves user to DB and replies with welcome</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify bot responds in Telegram</name>
  <files>src/bot.ts, src/index.ts</files>
  <action>
Human verification of bot functionality. The bot was built in Task 1 — this checkpoint confirms it works end-to-end in Telegram.
  </action>
  <verify>
    <automated>cd /Users/timur/energy-management && npx tsc --noEmit</automated>
  </verify>
  <done>User confirms: /start returns welcome message, text messages get echo response, menu button visible (if WEBAPP_URL set)</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles without errors
2. Bot starts and responds to /start in Telegram
3. Bot echoes arbitrary text messages
4. User record created in DB on /start
5. Menu button present (if WEBAPP_URL configured)
</verification>

<success_criteria>
- A message sent to the bot in Telegram receives a response
- /start command creates user in database and returns welcome
- Bot echoes text messages as acknowledgment
- Mini App menu button configured (when WEBAPP_URL is set)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-02-SUMMARY.md`
</output>

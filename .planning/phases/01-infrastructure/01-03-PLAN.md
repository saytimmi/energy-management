---
phase: 01-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/services/ai.ts
autonomous: true
requirements:
  - INFRA-04

user_setup:
  - service: anthropic
    why: "AI API for personalized energy advice"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> API Keys -> Create Key"

must_haves:
  truths:
    - "A call to the AI API returns a coherent response about energy management"
    - "AI service handles errors gracefully (no crashes on invalid key or network issues)"
  artifacts:
    - path: "src/services/ai.ts"
      provides: "AI service wrapper with energy-focused system prompt"
      exports: ["askAI"]
  key_links:
    - from: "src/services/ai.ts"
      to: "@anthropic-ai/sdk"
      via: "Anthropic client import"
      pattern: "new Anthropic"
    - from: "src/services/ai.ts"
      to: "src/config.ts"
      via: "API key from config"
      pattern: "config\\.anthropicApiKey"
---

<objective>
Create the AI API integration service. A wrapper around the Anthropic Claude API with an energy-management system prompt.

Purpose: INFRA-04 — AI API connected and returning responses. This service will be used by the bot intelligence in Phase 4.
Output: AI service module that can be called to get energy-related advice.
</objective>

<execution_context>
@/Users/timur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI service wrapper</name>
  <files>src/services/ai.ts</files>
  <action>
1. Create src/services/ai.ts:

   - Import Anthropic from @anthropic-ai/sdk
   - Import config from ../config

   - Create Anthropic client instance (uses ANTHROPIC_API_KEY from config)

   - Define SYSTEM_PROMPT constant (in Russian):
     "Ты — эксперт по управлению энергией. Ты работаешь с методологией 4 типов энергии: физическая, ментальная, эмоциональная, духовная. Каждый тип энергии требует своего способа восстановления — нельзя путать методы. Давай конкретные, практические советы. Отвечай кратко и по делу."

   - Export async function askAI(userMessage: string, context?: string): Promise<string>
     - Calls anthropic.messages.create with:
       - model: "claude-sonnet-4-20250514"
       - max_tokens: 1024
       - system: SYSTEM_PROMPT + (context ? "\n\nКонтекст пользователя: " + context : "")
       - messages: [{ role: "user", content: userMessage }]
     - Returns the text content from the response
     - Wraps in try/catch: on error, log the error and return a user-friendly fallback message: "Извини, не смог получить ответ от AI. Попробуй позже."

2. Verify the module compiles: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/timur/energy-management && npx tsc --noEmit 2>&1 | head -20 && echo "---COMPILE OK---" && npx tsx -e "
import { askAI } from './src/services/ai.js';
async function test() {
  const response = await askAI('Какие есть способы восстановить ментальную энергию?');
  console.log('AI Response length:', response.length);
  console.log('AI Response preview:', response.substring(0, 200));
  if (response.length > 10) { console.log('PASS'); } else { console.log('FAIL: response too short'); process.exit(1); }
}
test();
" 2>&1</automated>
  </verify>
  <done>AI service compiles, calling askAI() returns a coherent response about energy management, errors are caught gracefully</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — compiles without errors
2. askAI() call returns a response about energy management
3. Error handling works (bad key returns fallback message, not crash)
</verification>

<success_criteria>
- A call to the AI API returns a response without errors
- AI service has energy-focused system prompt in Russian
- Errors are handled gracefully with user-friendly fallback
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-03-SUMMARY.md`
</output>

---
phase: 01-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/services/scheduler.ts
  - src/index.ts
autonomous: true
requirements:
  - INFRA-05

must_haves:
  truths:
    - "Scheduler fires a test job at a configured interval without manual trigger"
    - "Scheduler can be started and stopped cleanly with the bot"
  artifacts:
    - path: "src/services/scheduler.ts"
      provides: "Cron scheduler service for periodic jobs"
      exports: ["startScheduler", "stopScheduler"]
  key_links:
    - from: "src/services/scheduler.ts"
      to: "node-cron"
      via: "cron.schedule import"
      pattern: "cron\\.schedule"
    - from: "src/index.ts"
      to: "src/services/scheduler.ts"
      via: "startScheduler call on boot"
      pattern: "startScheduler"
---

<objective>
Create the scheduler service that fires periodic jobs. For now, a test job that logs at a configured interval. In Phase 3, this will be wired to send morning/evening check-ins.

Purpose: INFRA-05 — Cron/scheduler fires reliably without manual trigger.
Output: Scheduler module that starts with the bot, runs cron jobs, and stops cleanly on shutdown.
</objective>

<execution_context>
@/Users/timur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-infrastructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scheduler service with test job</name>
  <files>src/services/scheduler.ts, src/index.ts</files>
  <action>
1. Create src/services/scheduler.ts:

   - Import cron from node-cron
   - Import { ScheduledTask } from node-cron types

   - Keep a list of active tasks: const tasks: ScheduledTask[] = []

   - Export function startScheduler():
     - Log "Scheduler started"
     - Create a test job that runs every minute ("* * * * *"):
       - Logs: "Scheduler heartbeat: {ISO timestamp}"
       - This proves the scheduler fires without manual trigger
     - Push task to tasks array
     - Also create placeholder commented-out entries for:
       - Morning check-in (e.g., "0 9 * * *" — 9:00 AM)
       - Evening check-in (e.g., "0 21 * * *" — 9:00 PM)
       - With TODO comments: "Wire to bot check-in handler in Phase 3"

   - Export function stopScheduler():
     - Iterate tasks, call task.stop() on each
     - Clear the tasks array
     - Log "Scheduler stopped"

2. Update src/index.ts:
   - Import { startScheduler, stopScheduler } from services/scheduler
   - Call startScheduler() after bot starts
   - Add stopScheduler() to the graceful shutdown handler (before bot.stop())

3. The full startup sequence in index.ts should be:
   - Load config
   - Connect to DB (prisma.$connect())
   - Start bot (bot.start())
   - Start scheduler (startScheduler())
   - Shutdown: stopScheduler() -> bot.stop() -> prisma.$disconnect()
  </action>
  <verify>
    <automated>cd /Users/timur/energy-management && npx tsc --noEmit 2>&1 | head -20 && echo "---COMPILE OK---" && timeout 70 npx tsx -e "
import { startScheduler, stopScheduler } from './src/services/scheduler.js';
let fired = false;
const origLog = console.log;
console.log = (...args: any[]) => {
  origLog(...args);
  const msg = args.join(' ');
  if (msg.includes('heartbeat')) { fired = true; stopScheduler(); console.log = origLog; console.log(fired ? 'PASS: scheduler fired' : 'FAIL'); process.exit(fired ? 0 : 1); }
};
startScheduler();
setTimeout(() => { stopScheduler(); console.log = origLog; console.log('FAIL: no heartbeat in 65s'); process.exit(1); }, 65000);
" 2>&1</automated>
  </verify>
  <done>Scheduler starts with the app, fires test heartbeat job every minute, stops cleanly on shutdown</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — compiles without errors
2. Scheduler fires heartbeat within 60 seconds of starting
3. Scheduler stops cleanly without hanging
4. Startup sequence: DB -> Bot -> Scheduler
</verification>

<success_criteria>
- Scheduler fires a test job at configured time without manual trigger
- Scheduler starts and stops cleanly with the application lifecycle
- Placeholder cron entries exist for morning/evening check-ins (Phase 3)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-04-SUMMARY.md`
</output>

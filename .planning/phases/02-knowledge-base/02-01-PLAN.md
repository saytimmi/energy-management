---
phase: 02-knowledge-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge/types.ts
  - src/knowledge/data.ts
  - src/knowledge/index.ts
  - src/knowledge/verify.ts
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03

must_haves:
  truths:
    - "Querying physical energy returns its recovery practices (son 7-9h, walking, etc.)"
    - "Querying mental energy returns its drain factors (multitasking, notifications, etc.)"
    - "Querying emotional energy returns its recovery practices (talk with close person, humor, etc.)"
    - "Querying spiritual energy returns its drain factors (meaningless work, cynicism, etc.)"
    - "Attempting to apply physical recovery to emotional burnout is rejected"
    - "Attempting to apply entertainment to spiritual emptiness is rejected"
    - "Spiritual energy can convert to any other type (special rule)"
    - "Knowledge base is importable as a module, not hardcoded in handlers"
  artifacts:
    - path: "src/knowledge/types.ts"
      provides: "EnergyType enum, Practice, DrainFactor, SubstitutionRule types"
      exports: ["EnergyType", "Practice", "DrainFactor", "SubstitutionRule"]
    - path: "src/knowledge/data.ts"
      provides: "All methodology data — practices, drains, substitution rules"
      contains: "RECOVERY_PRACTICES"
    - path: "src/knowledge/index.ts"
      provides: "Query functions for bot logic"
      exports: ["getRecoveryPractices", "getDrainFactors", "validateRecovery", "getSubstitutionRules"]
  key_links:
    - from: "src/knowledge/index.ts"
      to: "src/knowledge/data.ts"
      via: "imports data maps"
      pattern: "import.*from.*data"
    - from: "src/knowledge/index.ts"
      to: "src/knowledge/types.ts"
      via: "imports types"
      pattern: "import.*EnergyType.*from.*types"
---

<objective>
Encode the complete 4-energy methodology into a queryable TypeScript knowledge base module.

Purpose: Bot logic (Phase 3-4) needs to query recovery practices, drain factors, and validate that recommendations match the correct energy type. This plan creates that foundation as a standalone module.

Output: `src/knowledge/` module with types, all methodology data, query functions, and substitution validation.
</objective>

<execution_context>
@/Users/timur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define types and seed all methodology data</name>
  <files>src/knowledge/types.ts, src/knowledge/data.ts</files>
  <behavior>
    - EnergyType enum has exactly 4 values: physical, mental, emotional, spiritual
    - RECOVERY_PRACTICES map has entries for all 4 types
    - Physical recovery has at least 10 practices (son 7-9h, morning light, walking 8-12k, strength 2-4x/week, protein 1.6-2g/kg, water 30-40ml/kg, magnesium, sauna/contrast, breathing with long exhale, schedule/routine)
    - Mental recovery has at least 9 practices (90-min cycles, 10-20min defocus, look at water/fire/horizon, walk without phone, meditation 10-15min, nap 15-25min, write out tasks, 1 main task per day, reduce incoming flow)
    - Emotional recovery has at least 10 practices (talk with close person, laughter/humor, hugs, community, group sport, nature, gratitude 3 things/day, limit toxic people, sincere dialogue, crying)
    - Spiritual recovery has at least 9 practices (formulate mission, long-term goal, charity, help the weak, places of silence, prayer/philosophy, reflect on finality, deep books, create something eternal)
    - DRAIN_FACTORS map has entries for all 4 types with corresponding drains from the methodology
    - Physical drains include: lack of sleep, stress, alcohol, nicotine, sugar, water deficit, no movement, overeating, overtraining, prolonged sitting, jet lag
    - Mental drains include: multitasking, notifications, constant decisions, 3-5h continuous focus, scrolling, unfinished tasks, emotional conflicts, perfectionism, caffeine after 15:00
    - Emotional drains include: conflicts, toxic environment, complaints, feeling undervalued, pressure of expectations, comparison with others, public criticism, lack of support, isolation
    - Spiritual drains include: meaningless work, betrayal of values, living for others' opinions, cynicism, only consuming, no long-term goal, chasing status, dopamine mode
  </behavior>
  <action>
Create `src/knowledge/types.ts`:
- Define `EnergyType` enum with 4 values: `physical`, `mental`, `emotional`, `spiritual`
- Define `Practice` interface: `{ id: string; name: string; description: string; energyType: EnergyType }`
- Define `DrainFactor` interface: `{ id: string; name: string; description: string; energyType: EnergyType }`
- Define `SubstitutionRule` interface: `{ fromType: EnergyType; toType: EnergyType; allowed: boolean; reason: string }`

Create `src/knowledge/data.ts`:
- Import types from `./types.js`
- Create `RECOVERY_PRACTICES: Map<EnergyType, Practice[]>` with ALL practices from the methodology (see behavior section for complete lists per type). Use short kebab-case ids (e.g., `sleep-7-9h`, `morning-light`). Name in Russian matching the source. Description expands on the practice in Russian.
- Create `DRAIN_FACTORS: Map<EnergyType, DrainFactor[]>` with ALL drain factors from the methodology (see behavior section for complete lists). Same id/naming conventions.
- Create `SUBSTITUTION_RULES: SubstitutionRule[]` encoding these rules:
  - physical -> emotional: NOT allowed ("Нельзя лечить эмоциональное выгорание спортом")
  - emotional -> spiritual: NOT allowed ("Нельзя лечить духовную пустоту развлечениями")
  - mental -> mental-wrong: NOT allowed ("Нельзя лечить ментальную перегрузку кофе" — caffeine is a drain, not recovery)
  - physical -> physical-wrong: NOT allowed ("Нельзя лечить физическое истощение мотивацией")
  - General rule: recovery type MUST match drain type (same-type only)
  - Exception: spiritual recovery CAN convert to any type ("Духовная энергия может конвертироваться в любую другую")
- Export all three data structures.

All text content MUST be in Russian to match the bot's language.
  </action>
  <verify>
    <automated>cd /Users/timur/energy-management && npx tsc --noEmit src/knowledge/types.ts src/knowledge/data.ts</automated>
  </verify>
  <done>types.ts exports all 4 type definitions, data.ts contains complete methodology data for all 4 energy types with all practices and drains from PROJECT.md, substitution rules encode all 4 prohibitions plus the spiritual exception</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Query functions and verification script</name>
  <files>src/knowledge/index.ts, src/knowledge/verify.ts</files>
  <behavior>
    - getRecoveryPractices(EnergyType.physical) returns array of 10+ practices
    - getRecoveryPractices(EnergyType.mental) returns array of 9+ practices
    - getRecoveryPractices(EnergyType.emotional) returns array of 10+ practices
    - getRecoveryPractices(EnergyType.spiritual) returns array of 9+ practices
    - getDrainFactors(EnergyType.physical) returns array of 11+ drain factors
    - validateRecovery(EnergyType.emotional, EnergyType.physical) returns { allowed: false, reason: "..." } (can't fix emotional with physical/sport)
    - validateRecovery(EnergyType.physical, EnergyType.physical) returns { allowed: true }
    - validateRecovery(EnergyType.physical, EnergyType.spiritual) returns { allowed: true } (spiritual converts to any)
    - validateRecovery(EnergyType.mental, EnergyType.emotional) returns { allowed: false }
    - getAllEnergyTypes() returns all 4 types
    - getEnergyOverview(EnergyType) returns { practices: Practice[], drains: DrainFactor[] }
  </behavior>
  <action>
Create `src/knowledge/index.ts`:
- Import data and types from `./data.js` and `./types.js`
- Re-export all types from `./types.js`
- `getRecoveryPractices(type: EnergyType): Practice[]` — returns practices for given type
- `getDrainFactors(type: EnergyType): DrainFactor[]` — returns drain factors for given type
- `validateRecovery(drainType: EnergyType, recoveryType: EnergyType): { allowed: boolean; reason?: string }` — checks if applying recoveryType practices to drainType problems is valid. Rules:
  1. Same type -> always allowed
  2. Spiritual recovery -> any drain type: allowed (spiritual converts)
  3. Any other cross-type -> NOT allowed, return reason explaining why
- `getAllEnergyTypes(): EnergyType[]` — returns all 4 types
- `getEnergyOverview(type: EnergyType): { practices: Practice[], drains: DrainFactor[] }` — convenience for bot to get full picture for one type
- `getSubstitutionRules(): SubstitutionRule[]` — returns all rules for reference

Create `src/knowledge/verify.ts`:
- A runnable verification script (`npx tsx src/knowledge/verify.ts`)
- Imports all query functions
- Runs these checks, printing PASS/FAIL for each:
  1. Each of 4 types has recovery practices (length > 0)
  2. Each of 4 types has drain factors (length > 0)
  3. Physical practices count >= 10
  4. Mental practices count >= 9
  5. Emotional practices count >= 10
  6. Spiritual practices count >= 9
  7. validateRecovery(physical, physical) -> allowed
  8. validateRecovery(emotional, physical) -> NOT allowed (can't fix emotional with sport)
  9. validateRecovery(spiritual, spiritual) -> allowed
  10. validateRecovery(physical, spiritual) -> allowed (spiritual converts)
  11. validateRecovery(mental, emotional) -> NOT allowed
  12. validateRecovery(emotional, mental) -> NOT allowed
- Exits with code 0 if all pass, code 1 if any fail
- Prints summary: "X/Y checks passed"
  </action>
  <verify>
    <automated>cd /Users/timur/energy-management && npx tsc --noEmit && npx tsx src/knowledge/verify.ts</automated>
  </verify>
  <done>All query functions work correctly, verification script runs 12 checks and all pass, cross-type substitutions are rejected except spiritual-to-any, knowledge base is a clean importable module</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — full project compiles without errors
2. `npx tsx src/knowledge/verify.ts` — all 12 checks pass
3. Knowledge base is importable: `import { getRecoveryPractices, validateRecovery } from './knowledge/index.js'`
</verification>

<success_criteria>
- For each of 4 energy types, getRecoveryPractices returns the complete list from the methodology
- For each of 4 energy types, getDrainFactors returns the complete list from the methodology
- validateRecovery rejects cross-type substitutions (emotional+physical, mental+coffee, etc.)
- validateRecovery allows same-type and spiritual-to-any
- All data is in Russian matching the source methodology
- Module is self-contained in src/knowledge/, not hardcoded in bot handlers
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-base/02-01-SUMMARY.md`
</output>

---
phase: 03-bot-check-in-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/handlers/energy.ts
  - src/bot.ts
  - src/handlers/help.ts
autonomous: true
requirements:
  - BOT-03

must_haves:
  truths:
    - "User can message the bot at any time and log their current energy level"
    - "On-demand energy log is stored with logType 'manual' and correct timestamp"
    - "User gets confirmation with the recorded values"
  artifacts:
    - path: "src/handlers/energy.ts"
      provides: "Handler for /energy command and on-demand energy logging"
      min_lines: 30
    - path: "src/bot.ts"
      provides: "Registered /energy command and updated command list"
    - path: "src/handlers/help.ts"
      provides: "Updated help text with /energy and /checkin commands"
  key_links:
    - from: "src/handlers/energy.ts"
      to: "src/handlers/checkin.ts"
      via: "reuses sendCheckInMessage to start the same inline keyboard flow"
      pattern: "sendCheckInMessage"
    - from: "src/bot.ts"
      to: "src/handlers/energy.ts"
      via: "bot.command registration"
      pattern: 'bot\\.command\\("energy"'
---

<objective>
Add on-demand energy logging so users can record their energy levels at any time, not just during scheduled check-ins.

Purpose: Users should not be limited to morning/evening windows. They need to capture energy state whenever they notice a shift.
Output: /energy command that triggers the same inline keyboard rating flow with logType "manual".
</objective>

<execution_context>
@/Users/timur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bot-check-in-loop/03-01-SUMMARY.md

@src/bot.ts
@src/handlers/checkin.ts
@src/handlers/help.ts

<interfaces>
<!-- From 03-01 plan — the check-in handler exports -->

From src/handlers/checkin.ts (created in 03-01):
```typescript
export function sendCheckInMessage(chatId: number, logType: "morning" | "evening" | "manual"): Promise<void>;
export function handleCheckinCallback(ctx: CallbackQueryContext<Context>): Promise<void>;
```

Note: sendCheckInMessage may need its logType parameter widened to include "manual" if not already done in 03-01. If the type is already `string`, it works as-is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /energy command for on-demand logging</name>
  <files>src/handlers/energy.ts, src/bot.ts, src/handlers/help.ts</files>
  <action>
Create src/handlers/energy.ts:
1. Import `sendCheckInMessage` from "./checkin.js" and grammY types.
2. Export `async function energyHandler(ctx: CommandContext<Context>)` — calls `sendCheckInMessage(ctx.chat.id, "manual")` to reuse the same inline keyboard flow. The check-in handler already handles storing EnergyLog with the provided logType.
3. If ctx.from is missing, reply with "Не удалось определить пользователя." and return.

Update src/bot.ts:
1. Import `energyHandler` from "./handlers/energy.js"
2. Add `bot.command("energy", energyHandler)` BEFORE the message:text handler
3. Update setMyCommands in setupBot to include: `{ command: "energy", description: "Записать энергию прямо сейчас" }`
4. Update the generic message:text handler to hint about /energy command: change reply to `"Используй /energy чтобы записать уровень энергии, или /help для списка команд."`

Update src/handlers/help.ts:
1. Add /energy and /checkin to the help text:
   - "/energy — Записать уровень энергии прямо сейчас"
   - "/checkin — Начать check-in (как утренний/вечерний)"
2. Keep existing /start and /help entries.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>/energy command triggers the inline keyboard flow with logType "manual". Help text lists all available commands. Generic text handler directs users to /energy.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — project compiles without errors
2. Manual: Send /energy to bot — inline keyboard for rating 4 energies appears
3. Manual: Complete rating — EnergyLog with logType "manual" created in DB
4. Manual: Send /help — all commands listed
</verification>

<success_criteria>
- /energy command starts the 4-energy rating flow with logType "manual"
- Completed on-demand rating creates EnergyLog record in database
- Help text includes all commands (/start, /help, /energy, /checkin)
- Bot compiles and starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-bot-check-in-loop/03-02-SUMMARY.md`
</output>
